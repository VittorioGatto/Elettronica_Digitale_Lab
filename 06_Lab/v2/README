l'unico file aggiunto è "tester.vhd", fa solo lettura e scrittura di file. Guardate della waveform solo la prima parte prima del reset, il resto è merda.

Aggiunto tester_mem, nient'altro che un file di verifica del funzionamento delle mem con i file
